FROM node:22-slim

WORKDIR /app

# Install dependencies in a way that doesn't conflict with host
COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 5173

# Ensure we use localhost-accessible host
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
